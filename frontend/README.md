Instruções rápidas

1. Abra um terminal em `frontend`.
2. Rode `npm install` para instalar dependências.
3. Rode `npm run dev` para iniciar o Vite.

Observações

- O frontend assume que o backend está em http://localhost:3000
- Tailwind é carregado via CDN para simplificar (sem build extra)
